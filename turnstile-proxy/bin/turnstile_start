#!/bin/bash
SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
PIDFILE=$SCRIPT_DIR/../turnstile_proxy.pid


cd $SCRIPT_DIR/../turnstile_proxy
. /app/turnstile-proxy/.venv/bin/activate
./turnstile_proxy.py turnstile_proxy.yaml "$@"


